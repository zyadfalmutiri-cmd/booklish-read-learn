import type { ReactNode } from "react";

export function RequireAuth({ children }: { children: ReactNode }) {
  // ⚠️ مؤقتًا معطّل لمراجعة AdSense — رجّع الكود الأصلي بعدها
  return <>{children}</>;
}
