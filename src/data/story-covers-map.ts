/**
 * خريطة مركزية: slug القصة → رابط صورة الغلاف المولّدة
 *
 * كيف تستخدمه:
 * 1. افتح صفحة /admin/generate-covers وولّد الأغلفة
 * 2. اضغط زر "نسخ الكود الجاهز" بالصفحة
 * 3. الصق المحتوى المنسوخ هنا بدل السطر الفارغ تحت (استبدل كامل الكائن)
 * 4. سوِ commit — كل القصص بتاخذ غلافها تلقائيًا بدون لمس أي ملف قصة فردي
 */

export const storyCoverUrls: Record<string, string> = {
  // سيتم استبدال هذا السطر بالروابط المنسوخة من صفحة الأدمن
};
